// The context root for AJAX calls, links, and such
// TODO: detect this from the URL.
var WHITELAB_ROOT_PATH = "/opensonar_whitelab";

// The whole base url on the current server with the current protocol
var WHITELAB_BASE_URL = "//" + window.location.host + WHITELAB_ROOT_PATH;